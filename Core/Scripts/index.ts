import { add } from './add.script';
import { build } from './build.script';
import { link } from './link.script';
import { publish } from './publish.script';
import { remove } from './remove.script';
import { reset } from './reset.script';
import { start } from './start.script';

export const scripts = {
    add,
    build,
    link,
    publish,
    remove,
    reset,
    start,
}